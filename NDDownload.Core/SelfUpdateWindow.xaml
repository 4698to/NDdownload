<Window x:Class="NDDownload.SelfUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="安装器更新"
        Height="160" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Topmost="True"
        ShowInTaskbar="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="StatusText" Text="准备更新…" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <ProgressBar x:Name="Progress" Grid.Row="1" Height="22" Minimum="0" Maximum="100" Value="0" IsIndeterminate="False"/>
        <TextBlock x:Name="PercentText" Grid.Row="2" Margin="0,8,0,0" HorizontalAlignment="Right" Text="0%"/>
    </Grid>
</Window>
